services:
  - type: web
    name: techcards-pro
    runtime: node
    buildCommand: npm install && npx vite build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: BOT_TOKEN
        sync: false
      - key: PORT
        value: 3001
